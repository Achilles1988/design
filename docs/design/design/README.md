# Design specs for this app

Visual rules live in `rules/design.md`. One or more layout contracts live under `layouts/<id>/` (each with `LAYOUT.md`).

Installed and updated by the `wn-design-spec` skill. Do not hand-edit copies to match a preset — re-run install instead.
